name: mock_gps_app
description: Simulador GPS Pro 2.0
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^6.0.1
  latlong2: ^0.9.0
  http: ^1.1.0
  utm: ^2.0.0
  permission_handler: ^11.0.1
  shared_preferences: ^2.2.3
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
